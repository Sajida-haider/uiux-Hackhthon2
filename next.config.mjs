/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      // Local images automatically served from public folder
      // No need to add domains if using local images
      // If using external URLs, add their domains here
    },
    reactStrictMode: true, // Optional, for strict mode in React
  };
  
  export default nextConfig;